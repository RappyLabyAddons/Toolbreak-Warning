Component {
  font-size: 0.8;
}

// TODO: finish
.config-container {
  height: 300;
  width: 300;

  .config-area {
    height: fit-content;
    width: fit-content;
  }

  .type-selection-area {
    height: fit-content;
    width: 100%;
  }
}
}